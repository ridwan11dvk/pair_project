<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Pasien</title>
<body>
    <%- include('partials/navigation') %>
    <h1>Add Pasien</h1>
    <a href="/pasien">Back to Pasien Page</a>
    <br>
    <br>
    <br>

    <form action="/pasien/<%= data.id %>/edit" method="POST">
        <label>Nama</label><br>
        <br>
        <input type="text" name="nama" value="<%= data.nama %>"><br>
        <br>
        <label>Gender</label><br>
        <br>
        <input type="radio" name="gender" value="male" <%= data.gender == 'male' ? 'checked' : '' %>>
        <labdata>Male</label>
        <input type="radio" name="gender" value="female" <%= data.gender == 'female' ? 'checked' : '' %>>
        <label>Female</label>
        <br>
        <br>
        <label>Age</label><br>
        <br>
        <input type="text" name="age" value="<%= data.age %>"><br>
        <br>
        <br>
        <button type="submit">Save</button><a href="/pasien">Cancel</a>
    </form>
</body>
</html>